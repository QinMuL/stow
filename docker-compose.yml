services:
  stow:
    image: ghcr.io/qinmul/stow:latest
    container_name: stow
    restart: unless-stopped
    init: true
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./media:/app/media
